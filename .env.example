# Copy to .env and adjust if your deployment uses different hostnames.
# All values are public and end up in the production bundle.

VITE_OAUTH_CLIENT_ID=https://decay.cards/client-metadata.json
VITE_RENDER_SHELL_URL=https://decaycards.emnudge.dev/
VITE_RENDER_SHELL_PARENT_ORIGIN=https://decay.cards
